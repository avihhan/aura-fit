# Aura Fit — Flask API

Backend API for both browser clients: `mobile/` and `web-portal/`.

## Setup (Windows)

```bash
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
copy .env.example .env
```

## Run

```bash
python run.py
```

API base: `http://localhost:5000`
