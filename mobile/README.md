# Aura Fit — Mobile Web

This package is now a React + Vite web app (not React Native).

## Setup

```bash
npm install
copy .env.example .env
```

## Run

```bash
npm run dev
```

## Env keys

- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `VITE_API_URL`
